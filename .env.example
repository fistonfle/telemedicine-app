# API Configuration
# Leave empty to use Vite dev proxy (proxies /auth and /api to localhost:8088).
VITE_API_URL=

# Use mock data instead of real API (for development without backend)
# VITE_USE_MOCK_API=true
